# import the math module, containing features with mathematics
import math

print(f"The square root of 16 is {math.sqrt(16)}")
from math import pi
print(f"Pi is {pi}")

# import a module and give it a different name
import random as r
print(r.randint(100,200))

# there are also other 3rd party modules that you can install and use. 
# need to install (pip) to install them.
# one example was the tabulate module.